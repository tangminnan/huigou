<view class="container container-white">
	<view class="class-list">
		<view class="class-item {{classifyId[item.id] ? 'class-item__selected': ''}}" wx:for="{{classList}}" wx:key="name"
			data-key="{{item.id}}" bindtap="onSelectMerchantClass">{{item.name}}</view>
	</view>
	<view class="next-btn" bindtap="onSubmitApplyInfo">确定</view>
</view>